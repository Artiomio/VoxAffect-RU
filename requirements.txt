# Keep the base environment CPU-friendly. Do not use datasets[audio] here:
# recent versions can pull torch/CUDA dependencies transitively.
datasets>=2.18.0
soundfile>=0.12.1
librosa>=0.10.1
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.66
